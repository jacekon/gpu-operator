---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: gpuoperators.operator.kyma-project.io
spec:
  group: operator.kyma-project.io
  names:
    kind: GpuOperator
    listKind: GpuOperatorList
    plural: gpuoperators
    singular: gpuoperator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.driverVersion
      name: Driver Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GpuOperator is the Schema for the gpuoperators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GpuOperatorSpec defines the desired state of GpuOperator
            properties:
              driverVersion:
                default: "570"
                description: |-
                  DriverVersion specifies the NVIDIA driver version to install
                  Compatible with Garden Linux kernel versions in Kyma clusters
                type: string
              namespace:
                default: gpu-operator
                description: Namespace where the GPU operator will be installed
                type: string
              resources:
                description: Resources defines resource limits for GPU operator components
                properties:
                  limits:
                    description: Limits defines the maximum resources for the operator
                    properties:
                      cpu:
                        description: CPU resource requirement
                        type: string
                      memory:
                        description: Memory resource requirement
                        type: string
                    type: object
                  requests:
                    description: Requests defines the minimum resources for the operator
                    properties:
                      cpu:
                        description: CPU resource requirement
                        type: string
                      memory:
                        description: Memory resource requirement
                        type: string
                    type: object
                type: object
              valuesConfigMapName:
                description: |-
                  ValuesConfigMapName is the name of the ConfigMap containing custom Helm values
                  If specified, these values will be used instead of the default values
                type: string
            type: object
          status:
            description: GpuOperatorStatus defines the observed state of GpuOperator
            properties:
              conditions:
                description: |-
                  Conditions contain a set of conditionals to determine the State of Status.
                  If all Conditions are met, State is expected to be in StateReady.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              installedVersion:
                description: InstalledVersion is the version of the GPU operator currently
                  installed
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the GpuOperator
                  CR that was last processed
                format: int64
                type: integer
              state:
                description: |-
                  State signifies current state of Module CR.
                  Value can be one of ("Ready", "Processing", "Error", "Deleting").
                enum:
                - Processing
                - Deleting
                - Ready
                - Error
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
